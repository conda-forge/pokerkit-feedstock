schema_version: 1

context:
  python_min: 3.11
  name: pokerkit
  version: "0.7.2"

package:
  name: ${{ name|lower }}
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/${{ name[0] }}/${{ name }}/pokerkit-${{ version }}.tar.gz
  sha256: 4e7e4745c602100e1a2ad25b0391f684b40c527af91cdb5ed30cce55308751f3

build:
  number: 0
  noarch: python
  script: ${{ PYTHON }} -m pip install . -vv

requirements:
  host:
    - python ${{ python_min }}.*
    - pip
    - setuptools
  run:
    - python >=${{ python_min }}

tests:
  - python:
      imports:
        - pokerkit
      pip_check: true
      python_version: ${{ python_min }}.*

about:
  summary: A Python package for various poker tools
  license: MIT
  license_file: LICENSE
  homepage: https://github.com/uoftcprg/pokerkit

extra:
  recipe-maintainers:
    - thewchan
